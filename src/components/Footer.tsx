import useCart from "../hooks/useCart"

type FooterProps = {
  viewCart: boolean,
}

const Footer = ({ viewCart }: FooterProps) => {
  return (
    <div>
      footer
    </div>
  )
}

export default Footer
